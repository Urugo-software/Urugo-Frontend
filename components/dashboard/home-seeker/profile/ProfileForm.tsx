"use client";
import { useState } from "react";
const initial = { name: "Yves Kamanzi", phone: "0788 445 221", email: "yves.k@gmail.com" };
export function ProfileForm() {
  const [values, setValues] = useState(initial); const changed = JSON.stringify(values) !== JSON.stringify(initial);
  return <section className="mb-6 rounded-2xl border border-line p-6"><div className="mb-6 flex items-center gap-4"><span className="grid size-16 place-items-center rounded-full bg-brand-tint text-xl font-extrabold text-brand">YK</span><div><h2 className="text-base font-extrabold">Yves Kamanzi</h2><p className="text-sm text-faint">Guest account</p></div></div><div className="grid gap-5 sm:grid-cols-2">{Object.entries(values).map(([key, value]) => <label key={key} className={key === "email" ? "sm:col-span-2" : ""}><span className="mb-2 block text-sm font-bold">{key === "name" ? "Full name" : key === "phone" ? "Phone number" : "Email address"}</span><input type={key === "email" ? "email" : "text"} value={value} onChange={(event) => setValues({ ...values, [key]: event.target.value })} className="w-full rounded-lg border border-line px-3.5 py-2.5 text-sm outline-none transition focus:border-brand focus:ring-2 focus:ring-brand/15" /></label>)}</div><div className="mt-6 flex justify-end gap-2"><button onClick={() => setValues(initial)} disabled={!changed} className="rounded-lg border border-line px-4 py-2 text-sm font-semibold text-body disabled:opacity-50">Cancel</button><button disabled={!changed} className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white hover:bg-brand-deep disabled:cursor-default disabled:opacity-50">Save changes</button></div></section>;
}