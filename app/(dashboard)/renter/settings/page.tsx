import { RenterSettingsView } from "@/components/dashboard/renter/settings/RenterSettingsView";

export default function RenterSettingsPage() {
  return (
    <main className="flex flex-1 flex-col min-h-0 min-w-0 overflow-hidden bg-white h-full">
      <RenterSettingsView />
    </main>
  );
}
