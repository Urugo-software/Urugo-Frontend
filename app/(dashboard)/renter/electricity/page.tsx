import { RenterElectricityView } from "@/components/dashboard/renter/electricity/RenterElectricityView";

export default function RenterElectricityPage() {
  return (
    <main className="flex flex-1 flex-col min-h-0 min-w-0 overflow-hidden bg-white h-full">
      <RenterElectricityView />
    </main>
  );
}
